import React from "react";
import "../styles.css";

// import { gsap } from "gsap";
// import { SplitText } from "gsap/all";

// const title = gsap.utils.toArray('p')
// const tl = gsap.timeline()

// title.forEach(title => {
//     const splitTitle = new SplitTextJS(title);
// })

function Hero() {
    return (
        <div>
            <h2 className="hero--text">Hero</h2>
        </div>
    )
}

export default Hero